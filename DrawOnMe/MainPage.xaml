<phone:PhoneApplicationPage
    x:Class="DrawOnMe.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c4f="clr-namespace:Coding4Fun.Toolkit.Controls;assembly=Coding4Fun.Toolkit.Controls"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    xmlns:converters="clr-namespace:DrawOnMe.Converters"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True" Loaded="PhoneApplicationPage_Loaded">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.Resources>
            <converters:ContrastColorConverter x:Name="ContrastColorConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="12 0 12 5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="DRAW ON ME" Margin="0 0 30 0" Style="{StaticResource PhoneTextNormalStyle}"/>
           
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="45" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" FontSize="15" Text="Bg color" TextAlignment="Center" Margin="0 0 7 0" />
                <Ellipse StrokeThickness="2" Stroke="{Binding BgColor, Converter={StaticResource ContrastColorConverter}}" x:Name="bgColorEllipse" Grid.Column="0" Grid.Row="1" Margin="28 2" Fill="{Binding BgColor}" />

                <TextBlock Grid.Column="1" Grid.Row="0" FontSize="15" Text="Line color" TextAlignment="Center" Margin="0 0 7 0" />
                <Ellipse StrokeThickness="2" Stroke="{Binding LineColor, Converter={StaticResource ContrastColorConverter}}" x:Name="lineColorEllipse" Grid.Column="1" Grid.Row="1" Margin="28 2" Fill="{Binding LineColor}" />

                <TextBlock Grid.Column="2" Grid.Row="0" FontSize="15" Text="Line thickness" TextAlignment="Center" />
                <Grid Grid.Column="2" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" x:Name="thicknessMinus" Tap="thicknessMinus_Tap">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Rectangle Grid.Column="0" Grid.Row="0" Stroke="{StaticResource PhoneForegroundBrush}" />
                        <TextBlock Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" TextAlignment="Center" Text="-" />
                    </Grid>
                    
                    <TextBlock x:Name="thicknessValueTextBox" Grid.Column="1" Text="{Binding Thickness}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    
                    <Grid Grid.Column="3" x:Name="thicknessPlus" Tap="thicknessPlus_Tap">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Rectangle Grid.Column="0" Grid.Row="0" Stroke="{StaticResource PhoneForegroundBrush}" />
                        <TextBlock Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" TextAlignment="Center" Text="+" />
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

        <Canvas Grid.Row="1" x:Name="paint" Background="White" Margin="10,0, 10, 10" />
    </Grid>

</phone:PhoneApplicationPage>